apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - controller.yaml
  - acme-storage.yaml
  - argocd-webhook-ingress.yaml
  - argocd-webhook-backend.yaml
  - argocd-webhook-transport.yaml
